// Keys sourced from gin/js/toolbar.js and gin/js/navigation/navigation.js.
if (!localStorage.getItem('Drupal.gin.toolbarExpanded')) {
  localStorage.setItem('Drupal.gin.toolbarExpanded', 'false');
}
if (!localStorage.getItem('Drupal.navigation.sidebarExpanded')) {
  localStorage.setItem('Drupal.navigation.sidebarExpanded', 'false');
}
if (!localStorage.getItem('Drupal.gin.sidebarExpanded.desktop')) {
  localStorage.setItem('Drupal.gin.sidebarExpanded.desktop', 'false');
}
if (!localStorage.getItem('Drupal.gin.sidebarExpanded.mobile')) {
  localStorage.setItem('Drupal.gin.sidebarExpanded.mobile', 'false');
}
