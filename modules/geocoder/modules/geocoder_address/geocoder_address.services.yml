services:
  geocoder_address.address:
    class: Drupal\geocoder_address\AddressService
    arguments:
      [
        "@address.address_format_repository",
        "@address.country_repository",
        "@address.subdivision_repository",
        "@module_handler"
      ]
  Drupal\geocoder_address\AddressService: '@geocoder_address.address'
