services:

  _defaults:
    autowire: true

  # @todo Remove when minimum version is ^11.1, as it will then be automatic.
  Drupal\genpass_batch\Hook\UserInsertBatch:
    class: Drupal\genpass_batch\Hook\UserInsertBatch

# All procedural hooks converted to OOP hooks.
# @see https://www.drupal.org/node/3490771
parameters:
  genpass_batch.hooks_converted: true
