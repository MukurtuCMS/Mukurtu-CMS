services:
  mukurtu_community_records.create_access_check:
    class: Drupal\mukurtu_community_records\Access\AddCommunityRecordAccessCheck
    arguments: ['@current_user']
    tags:
      - # Required: Mark that this service is for access checks.
        name: access_check,
        # Optional: Specify which routes this applies to.
        applies_to: _create_community_record_access_check
