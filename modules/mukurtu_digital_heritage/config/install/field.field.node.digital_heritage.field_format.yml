langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_format
    - node.type.digital_heritage
    - taxonomy.vocabulary.format
id: node.digital_heritage.field_format
field_name: field_format
entity_type: node
bundle: digital_heritage
label: Format
description: 'Format is the specific physical or digital manifestation of the Media Asset or Digital Heritage Item. Include physical format (eg: pamphlet, glass slide, open reel), duration or extent (eg: 90 minutes, 20 pages), dimensions (eg: 4x6”, 12x6x8cm), digital filetype (eg: PDF, JPG, MP3, MP4), or other details as needed.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      format: format
    sort:
      field: name
      direction: asc
    auto_create: true
    auto_create_bundle: ''
field_type: entity_reference
