langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_subject
    - node.type.digital_heritage
    - taxonomy.vocabulary.subject
id: node.digital_heritage.field_subject
field_name: field_subject
entity_type: node
bundle: digital_heritage
label: Subject
description: 'The main topic or topics presented in the Digital Heritage Item. Subjects may be derived from existing classification systems (for example, Library of Congress Classification Numbers or Dewey Decimal numbers), controlled vocabularies (such as Medical Subject Headings or Art and Architecture Thesaurus descriptors), or can be created as needed within the site.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      subject: subject
    sort:
      field: name
      direction: asc
    auto_create: true
    auto_create_bundle: ''
field_type: entity_reference
