{#
/**
 * @file
 * Default theme implementation for media content warnings.
 *
 * Available variables:
 * - media: The media entity.
 * - warnings: The warnings.
 */
#}
<div {{ attributes.addClass('mukurtu-content-warnings').setAttribute('data-mid', media.id).setAttribute('role', 'button').setAttribute('tabindex', 0).setAttribute('aria-pressed', 'false') }}>
  {{ warnings }}
</div>
