{#
/**
 * @file
 * Default theme implementation to present a multipage item entity.
 *
 * This template is used when viewing a registered multipage item's page,
 * e.g., /admin/content/multipage-item)/123. 123 being the multipage item's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_multipage_item()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
