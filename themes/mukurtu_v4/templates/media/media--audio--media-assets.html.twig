{#
/**
 * @file
 * Theme override to present an audio file in the media assets field.
 *
 * Available variables:
 * - media: The media item, with limited access to object properties and
 *   methods. Only method names starting with "get", "has", or "is" and
 *   a few common methods such as "id", "label", and "bundle" are available.
 *   For example:
 *   - entity.getEntityTypeId() will return the entity type ID.
 *   - entity.hasField('field_example') returns TRUE if the entity includes
 *     field_example. (This does not indicate the presence of a value in this
 *     field.)
 *   Calling other methods, such as entity.delete(), will result in
 *   an exception.
 *   See \Drupal\Core\Entity\EntityInterface for a full list of methods.
 * - name: Name of the media item.
 * - content: Media content.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - view_mode: View mode; for example, "teaser" or "full".
 * - attributes: HTML attributes for the containing element.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - bundle: machine name of media type.
 * @see template_preprocess_media()
 */
#}
{%
  set classes = [
    'media',
    'media--' ~ bundle
  ]
%}

<div{{ attributes.addClass(classes) }}>
  {{ title_suffix.contextual_links }}
  {{ content.media_content_warnings }}

  <span class="media-asset--content">
    <img alt="audio file {{ name }}" src="{{ audio_fallback_thumbnail_url }}">
  </span>

  {{ content.field_media_audio_file }}

  <span class="media-asset--name">{{ name }}</span>

  <div class="media-asset--actions">
    <a href="#audio-{{ media.id }}" data-type="inline" class="media-asset--link button" aria-label="Expand {{ name }} audio">Expand +</a>
    {% if media_download_enabled and media_url %}
      <a href="{{ media_url }}" class="button button--download" download aria-label="Download audio: {{ name }}">Download audio <svg xmlns="http://www.w3.org/2000/svg" xml:space="preserve" width="16" height="18" aria-hidden="true"><path d="M11.61 0H3.87v5.57H0L7.74 13l7.74-7.43h-3.87z"/><path fill-rule="evenodd" d="M.52 15H16v3H.52z" clip-rule="evenodd"/></svg></a>
    {% endif %}
  </div>

  <div id="audio-{{ media.id }}" class="media-asset--glightbox-inline">
    <img alt="audio file {{ name }}" src="{{ audio_fallback_thumbnail_url }}">
    {{ content.field_media_audio_file }}
  </div>

</div>
